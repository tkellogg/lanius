#!/bin/sh
# Demo handler: append each event envelope to echo.log in the harness root.
cat >> "${ELANUS_ROOT:-${HARNESS_ROOT:-.}}/echo.log"
printf '\n' >> "${ELANUS_ROOT:-${HARNESS_ROOT:-.}}/echo.log"
