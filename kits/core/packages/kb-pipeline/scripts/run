#!/bin/sh
# in/agent/kb-compactor | in/agent/kb-ratifier -> an agent turn under the profile
# named in the spawn payload (`payload.profile`, set by spawn_core). All event-JSON
# parsing lives in the binary; this script exists so the wiring stays visible in
# handlers.d/, exactly like packages/chat/scripts/run.
exec elanus handle-exec
